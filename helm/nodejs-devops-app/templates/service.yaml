apiVersion: v1
kind: Service
metadata:
  name: {{ include "nodejs-devops-app.fullname" . }}
  labels:
    {{- include "nodejs-devops-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nodejs-devops-app.selectorLabels" . | nindent 4 }}